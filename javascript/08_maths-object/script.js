const width = prompt("pleae enter width of rectengle: ")
const length = prompt("pleae enter length of rectengle: ")

console.log(length * width);
console.log(2(length + width));

document.writeln(`rectengle area: ${length * width}`);
document.writeln(`rectengle perimeter: ${2(length + width)}`);